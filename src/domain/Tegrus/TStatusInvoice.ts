export enum TStatusInvoice {
    CANCELADO = 'CANCELED',
    PUBLICADO = 'ISSUED',
    PAGO = 'PAID',
}

export enum EnumTopicStatusInvoice {
    canceled = 'canceled',
    issued = 'issued',
    credit = 'credit',
}
