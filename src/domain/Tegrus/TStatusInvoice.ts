export enum TStatusInvoice {
    CANCELADO = 'CANCELED',
    PUBLICADO = 'ISSUED',
    PAGO = 'PAID',
}

export enum EnumTopicStatusInvoice {
    canceled = 'canceled',
    issued = 'issued',
    credit = 'credit',
    reject = 'reject',
    pay = 'pay',
    paused = 'paused',
}
